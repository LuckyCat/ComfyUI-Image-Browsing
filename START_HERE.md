# 🚀 Настройка путей - Быстрый старт

## Что было добавлено

Теперь вы можете настраивать пути для:
- **thumbnail_cache** - где хранится кеш превью изображений
- **output** - папка с выходными файлами
- **workflows** - папка с workflow файлами
- **prompts** - папка с файлами промптов

Аналогично настройке FFmpeg!

---

## Шаг 1: Запустите ComfyUI

```bash
cd x:\ComfyUI_windows_portable_worker
.\run_nvidia_gpu.bat
```

Или как вы обычно запускаете ComfyUI.

**Дождитесь полной загрузки** - в консоли должно появиться:
```
To see the GUI go to: http://127.0.0.1:8188
```

---

## Шаг 2: Проверьте, что расширение загрузилось

В консоли ComfyUI должно быть что-то вроде:
```
Import module: ComfyUI Image Browsing
```

Если видите ошибки Python - сообщите, какие именно.

---

## Шаг 3: Откройте ComfyUI в браузере

Перейдите на: http://127.0.0.1:8188

Откройте консоль разработчика (нажмите F12)

---

## Шаг 4: Протестируйте API

В консоли браузера выполните:

```javascript
// Проверка текущих путей
fetch('/image-browsing/paths/status')
  .then(r => r.json())
  .then(data => {
    console.log('Результат:', data);
    if (data.success) {
      console.log('✅ API работает!');
      console.table(data.data);
    } else {
      console.error('❌ Ошибка:', data.error);
    }
  })
  .catch(err => console.error('❌ Запрос не прошёл:', err));
```

### Если всё работает:

Вы увидите таблицу с 4 путями и их статусом:

```
thumbnail_cache:
  current: "x:\ComfyUI_windows_portable_worker\ComfyUI\custom_nodes\..."
  default: "x:\ComfyUI_windows_portable_worker\ComfyUI\custom_nodes\..."
  is_default: true
  exists: true
  valid: true

output:
  current: "x:\ComfyUI_windows_portable_worker\ComfyUI\output"
  ...

workflows:
  ...

prompts:
  ...
```

---

## Шаг 5: Измените путь (пример)

```javascript
// Установить свой путь для кеша превью
fetch('/image-browsing/paths/set', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    type: 'thumbnail_cache',
    path: 'D:/MyComfyCache'  // ← Ваш путь
  })
})
  .then(r => r.json())
  .then(data => {
    console.log('Результат:', data);
    if (data.success) {
      console.log('✅ Путь изменён!');
      console.log('Новый путь:', data.data.path);
    } else {
      console.error('❌ Ошибка:', data.error);
    }
  });
```

**Важно для Windows:** Используйте `/` или `\\\\` в путях:
- ✅ Правильно: `"D:/Cache"` или `"D:\\\\Cache"`
- ❌ Неправильно: `"D:\Cache"` (одинарный `\` не работает в JSON)

---

## Шаг 6: Проверьте файл конфигурации

Откройте файл:
```
x:\ComfyUI_windows_portable_worker\ComfyUI\custom_nodes\ComfyUI-Image-Browsing\paths_config.json
```

Должно быть примерно так:

```json
{
  "thumbnail_cache": "D:/MyComfyCache",
  "output": "x:/ComfyUI_windows_portable_worker/ComfyUI/output",
  "workflows": "x:/ComfyUI_windows_portable_worker/ComfyUI/user/default/workflows",
  "prompts": "x:/ComfyUI_windows_portable_worker/ComfyUI/user/default/prompts"
}
```

---

## Шаг 7: Перезапустите ComfyUI (только для thumbnail_cache)

Если вы изменили путь `thumbnail_cache`, нужен перезапуск, чтобы кеш начал использовать новый путь.

Для других путей (output, workflows, prompts) перезапуск НЕ требуется.

---

## 🐛 Если не работает

### Ошибка в консоли ComfyUI при запуске

**Посмотрите ошибку** - скорее всего это будет:
```
ModuleNotFoundError
ImportError
SyntaxError
```

**Решение:** Скопируйте полный текст ошибки и покажите мне.

---

### API возвращает 404

**Симптом:**
```javascript
GET http://127.0.0.1:8188/image-browsing/paths/status 404 (Not Found)
```

**Причина:** Расширение не загрузилось

**Решение:**
1. Проверьте консоль ComfyUI - есть ли ошибки загрузки?
2. Проверьте, что файлы на месте:
   ```bash
   ls x:\ComfyUI_windows_portable_worker\ComfyUI\custom_nodes\ComfyUI-Image-Browsing\py\paths_config.py
   ```
3. Перезапустите ComfyUI

---

### "Failed to fetch" в браузере

**Симптом:**
```javascript
Failed to fetch
```

**Причина:** ComfyUI не запущен или работает на другом порту

**Решение:**
- Проверьте, что ComfyUI запущен
- Проверьте адрес - может быть не 8188, а другой порт

---

### Путь не валидируется

**Симптом:**
```json
{ "success": false, "error": "Invalid path: Path must be absolute" }
```

**Причина:** Использован относительный путь

**Решение:** Используйте полный путь:
- ❌ `./cache`
- ✅ `D:/Cache` или `/home/user/cache`

---

## 📚 Дополнительная документация

- **[TEST_API.md](TEST_API.md)** - Подробные примеры тестирования API
- **[PATHS_CONFIG_GUIDE.md](PATHS_CONFIG_GUIDE.md)** - Полная документация по настройке путей
- **[SETTINGS_GUIDE.md](SETTINGS_GUIDE.md)** - Общий гид по настройкам расширения

---

## ✅ Что делать дальше

После успешного тестирования API, можно:

1. **Добавить UI** - создать панель настроек в интерфейсе (как для FFmpeg)
2. **Настроить свои пути** - переместить кеш на более быстрый диск
3. **Организовать файлы** - использовать удобную структуру папок для workflows и prompts

---

## 💬 Сообщите результат

После тестирования напишите:
- ✅ Работает - покажите вывод из консоли
- ❌ Не работает - покажите ошибку полностью

Это поможет понять, на каком этапе возникла проблема!
